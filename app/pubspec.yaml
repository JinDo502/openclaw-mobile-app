name: openclaw_mobile_app
description: OpenClaw mobile admin app.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.7.0

flutter:
  uses-material-design: true
